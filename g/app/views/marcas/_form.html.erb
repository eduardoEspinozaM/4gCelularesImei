
            <%= form_with(model: marca, local: true, class: 'form-horizontal') do |form| %>
                <% if marca.errors.any? %>
                <div id="alert alert-info">
                   <i class="zmdi zmdi-info-outline"></i>
                    <h2><%= pluralize(marca.errors.count, "error") %> prohibited this marca from being saved:</h2>
                    <ul>
                        <% marca.errors.full_messages.each do |message| %>
                        <li><%= message %></li>
                        <% end %>
                    </ul>
                </div>
                <% end %>
                <div class="form-group">
                    <%= form.label :nombre, class: 'col-md-2 control-label' %>
                    <div class="col-lg-10 col-md-10">
                        <%= form.text_field :nombre, id: :marca_nombre, class: 'form-control', placeholder: 'Nombre' %>
                    </div>
                </div>
                <div class="form-group">
                    <div class="col-offset-lg-2 pull-right">
                        <%= form.submit 'Guardar', class: 'btn btn-success btn-raised' %>
                    </div>
                </div>
                <hr>
            <% end %>