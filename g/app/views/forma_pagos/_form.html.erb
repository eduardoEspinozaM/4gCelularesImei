<%= form_with(model: forma_pago, local: true) do |form| %>
  <% if forma_pago.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(forma_pago.errors.count, "error") %> prohibited this forma_pago from being saved:</h2>

      <ul>
      <% forma_pago.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre, id: :forma_pago_nombre %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
